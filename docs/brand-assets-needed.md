# Waymark brand assets needed

The supplied identity reference is a raster presentation board, not production-ready logo artwork. Add these approved Open Waypoint assets under `apps/web/public/brand` when available:

- Horizontal logo SVG
- Mark-only SVG
- One-color SVG
- Dark-background variant
- Favicon
- PNG exports at 16, 32, 48, 192, and 512 px

Until then the application uses a clean textual “Waymark” wordmark, as specified.
